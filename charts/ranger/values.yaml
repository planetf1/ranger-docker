# Default values for ranger.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

image:
  ranger:
    repository: ranger-docker_ranger-admin
    tag: latest
    pullPolicy: IfNotPresent

  mariadb:
    repository: ranger-docker_mariadb
    tag: latest
    pullPolicy: IfNotPresent

  nameOverride: ""
  fullnameOverride: ""



